info:
  name: Planos_Put
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:8080/planos/2
  body:
    type: json
    data: |-
      {
        "nome": "Teste M3",
        "descricao": "M3 Testando",
        "valor": 99.90,
        "duracao": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
